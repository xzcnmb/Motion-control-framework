<UserControl
    x:Class="Sophon.UI.Views.SubViews.AddParamView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="350"
    d:DesignWidth="250"
    mc:Ignorable="d">

    <Border Background="White" CornerRadius="10">
        <StackPanel Margin="15">
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="20"
                FontWeight="Bold"
                Text="参数添加" />
            <hc:Divider Margin="0,10,0,10" LineStroke="LightGray" />

            <Grid>
                <Grid.Resources>
                    <Style BasedOn="{StaticResource TextBlockBaseStyle}" TargetType="TextBlock">
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="HorizontalAlignment" Value="Left" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                        <Setter Property="Margin" Value="0,0,10,0" />
                    </Style>
                    <Style BasedOn="{StaticResource TextBoxBaseStyle}" TargetType="TextBox">
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="Margin" Value="0,5" />
                    </Style>
                    <Style BasedOn="{StaticResource ComboBoxBaseStyle}" TargetType="ComboBox">
                        <Setter Property="Margin" Value="0,5" />
                    </Style>
                </Grid.Resources>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="80" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0">
                    <Run Text="参数分类" /><Run Foreground="Red" Text=" *" />
                </TextBlock>
                <ComboBox
                    Grid.Row="0"
                    Grid.Column="1"
                    IsEditable="True"
                    ItemsSource="{Binding AvailableCategories}"
                    Text="{Binding SelectedCategory, Mode=TwoWay}" />

                <TextBlock Grid.Row="1" Grid.Column="0">
                    <Run Text="参数名称" /><Run Foreground="Red" Text=" *" />
                </TextBlock>
                <TextBox
                    Grid.Row="1"
                    Grid.Column="1"
                    Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="2" Grid.Column="0">
                    <Run Text="参数值" /><Run Foreground="Red" Text=" *" />
                </TextBlock>
                <TextBox
                    Grid.Row="2"
                    Grid.Column="1"
                    Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock
                    Grid.Row="3"
                    Grid.Column="0"
                    Text="单位" />
                <TextBox
                    Grid.Row="3"
                    Grid.Column="1"
                    Text="{Binding Unit, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock
                    Grid.Row="4"
                    Grid.Column="0"
                    Text="参数描述" />
                <TextBox
                    Grid.Row="4"
                    Grid.Column="1"
                    Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="5" Grid.Column="0">
                    <Run Text="用户等级" /><Run Foreground="Red" Text=" *" />
                </TextBlock>
                <ComboBox
                    Grid.Row="5"
                    Grid.Column="1"
                    IsEditable="True"
                    ItemsSource="{Binding UserLevels}"
                    Text="{Binding SelectedLevel, Mode=TwoWay}" />
            </Grid>
            <hc:ButtonGroup
                Width="220"
                Margin="0,10,0,0"
                Style="{StaticResource ButtonGroupSolid}">
                <RadioButton Command="{Binding ConfirmCommand}" Content="确认" />
                <RadioButton Command="{Binding CancelCommand}" Content="取消" />
            </hc:ButtonGroup>
        </StackPanel>
    </Border>
</UserControl>