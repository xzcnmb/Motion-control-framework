<UserControl
    x:Class="Sophon.UI.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="#F4F6F9">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,18">
                <TextBlock Text="设备运行总览" FontSize="24" FontWeight="SemiBold" Foreground="#1F2937" />
                <TextBlock Text="{Binding CurrentOperatorText}" FontSize="13" Foreground="#64748B" Margin="0,5,0,0" />
            </StackPanel>

            <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,18">
                <Border Background="White" BorderBrush="#D9E1E8" BorderThickness="1" Padding="14" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="控制器" FontSize="12" Foreground="#64748B" />
                        <TextBlock Text="{Binding ConnectionStatusText}" FontSize="17" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,6,0,0" />
                        <TextBlock Text="{Binding DriverKindName}" FontSize="11" Foreground="#64748B" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </Border>
                <Border Background="White" BorderBrush="#D9E1E8" BorderThickness="1" Padding="14" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="活动报警" FontSize="12" Foreground="#64748B" />
                        <TextBlock Text="{Binding AlarmSummaryText}" FontSize="17" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,6,0,0" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
                <Border Background="White" BorderBrush="#D9E1E8" BorderThickness="1" Padding="14" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="操作权限" FontSize="12" Foreground="#64748B" />
                        <TextBlock Text="{Binding CurrentOperatorText}" FontSize="17" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,6,0,0" TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="页面权限按角色限制" FontSize="11" Foreground="#64748B" Margin="0,4,0,0" />
                    </StackPanel>
                </Border>
                <Border Background="#FFF7E6" BorderBrush="#FFD591" BorderThickness="1" Padding="14">
                    <StackPanel>
                        <TextBlock Text="现场状态" FontSize="12" Foreground="#8C5A00" />
                        <TextBlock Text="{Binding WelcomeSubtitle}" FontSize="14" FontWeight="SemiBold" Foreground="#8C5A00" Margin="0,6,0,0" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="280" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="运行监视" FontSize="16" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,0,0,10" />
                    <WrapPanel>
                        <Button Width="170" Height="44" Margin="0,0,10,10" Content="工站运行" Command="{Binding NavigateCommand}" CommandParameter="StationView" Style="{StaticResource ButtonPrimary}" />
                        <Button Width="170" Height="44" Margin="0,0,10,10" Content="实时报警" Command="{Binding NavigateCommand}" CommandParameter="AlarmCenterView" Style="{StaticResource ButtonDefault}" />
                        <Button Width="170" Height="44" Margin="0,0,10,10" Content="限位监控" Command="{Binding NavigateCommand}" CommandParameter="LimitMonitorView" Style="{StaticResource ButtonDefault}" />
                    </WrapPanel>
                    <TextBlock Text="工程维护" FontSize="16" FontWeight="SemiBold" Foreground="#1F2937" Margin="0,16,0,10" />
                    <WrapPanel>
                        <Button Width="170" Height="44" Margin="0,0,10,10" Content="轴调试" Command="{Binding NavigateCommand}" CommandParameter="AxisDebugView" Style="{StaticResource ButtonDefault}" />
                        <Button Width="170" Height="44" Margin="0,0,10,10" Content="点位示教" Command="{Binding NavigateCommand}" CommandParameter="TeachView" Style="{StaticResource ButtonDefault}" />
                        <Button Width="170" Height="44" Margin="0,0,10,10" Content="流程编辑" Command="{Binding NavigateCommand}" CommandParameter="FlowEditorView" Style="{StaticResource ButtonDefault}" />
                    </WrapPanel>
                </StackPanel>
                <Border Grid.Column="1" Background="White" BorderBrush="#D9E1E8" BorderThickness="1" Padding="16" Margin="18,0,0,0">
                    <StackPanel>
                        <TextBlock Text="操作提示" FontSize="15" FontWeight="SemiBold" Foreground="#1F2937" />
                        <TextBlock Text="主页只用于观察和导航。轴点动、示教、流程启动等动作会在对应页面按权限和设备状态校验。" FontSize="12" Foreground="#64748B" Margin="0,10,0,0" TextWrapping="Wrap" />
                        <Button Content="打开板卡配置（选型 → 轴参数）" Command="{Binding NavigateCommand}" CommandParameter="MotionCardConfigView" Style="{StaticResource ButtonDefault}" Height="36" Margin="0,18,0,0" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
